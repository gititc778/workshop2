apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-affinity-preferred
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1           #Higher weight = more preferred node 1-100
            preference:
              matchExpressions:
              - key: vmtype
                operator: In
                values:
                - highmem
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80