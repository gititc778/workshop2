apiVersion: v1
kind: Service
metadata:
  name: {{ include "itclabs.fullname" . }}
  labels:
    {{- include "itclabs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "itclabs.selectorLabels" . | nindent 4 }}
