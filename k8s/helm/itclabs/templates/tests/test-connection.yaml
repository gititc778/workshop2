apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itclabs.fullname" . }}-test-connection"
  labels:
    {{- include "itclabs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "itclabs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
